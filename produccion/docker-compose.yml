version: '3.8'

services:
  # --- BACKEND (Laravel) ---
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: proximarkt-api
    restart: always
    env_file: ./api/.env
    networks:
      - proxy_net
      - backend_net
    labels:
      - "traefik.enable=true"
      # Escucha en /api para separar el tráfico del frontend
      - "traefik.http.routers.backend.rule=Host(`proximarkt.francecentral.cloudapp.azure.com`) && PathPrefix(`/api`)"
      - "traefik.http.routers.backend.entrypoints=websecure"
      - "traefik.http.routers.backend.tls.certresolver=myresolver"
      - "traefik.http.services.backend.loadbalancer.server.port=80"

  # --- FRONTEND (Vue) ---
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: proximarkt-frontend
    restart: always
    networks:
      - proxy_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.rule=Host(`proximarkt.francecentral.cloudapp.azure.com`)"
      - "traefik.http.routers.frontend.entrypoints=websecure"
      - "traefik.http.routers.frontend.tls.certresolver=myresolver"
      - "traefik.http.services.frontend.loadbalancer.server.port=80"

  # --- BASE DE DATOS ---
  db:
    image: mysql:8.0
    container_name: proximarkt-db
    restart: always
    environment:
      MYSQL_DATABASE: proximarkt
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - backend_net

networks:
  proxy_net:
    external: true  # Esto le dice que use la red que ya creó Traefik
  backend_net:
    driver: bridge

volumes:
  db_data: