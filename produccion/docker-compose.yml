version: '3.8'

services:
  # --- FRONTEND (Nginx + Vue) ---
  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=https://proximarkt.francecentral.cloudapp.azure.com/api
    container_name: proximarkt-frontend
    restart: always
    networks:
      - proxy_net
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy_net"
      - "traefik.http.routers.frontend.rule=Host(`proximarkt.francecentral.cloudapp.azure.com`)"
      - "traefik.http.routers.frontend.entrypoints=websecure"
      - "traefik.http.routers.frontend.tls.certresolver=myresolver"
      - "traefik.http.services.frontend.loadbalancer.server.port=80"

  # --- BACKEND (Laravel PHP-FPM) ---
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: proximarkt-backend
    restart: always
    env_file: ../backend/.env 
    volumes:
      - backend_storage:/var/www/html/storage
    networks:
      - proxy_net
      - backend_net

  # --- BASE DE DATOS (MySQL) ---
  mysql:
    image: mysql:8.0
    container_name: proximarkt-db
    restart: always
    environment:
      # Debe coincidir con DB_DATABASE en tu .env
      MYSQL_DATABASE: proxi_markt
      # Contraseña del usuario root
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-root_password_seguro}
      # CREA EL USUARIO QUE PIDE LARAVEL
      MYSQL_USER: alumno
      # CONTRASEÑA PARA EL USUARIO 'alumno' (debe coincidir con DB_PASSWORD en .env)
      MYSQL_PASSWORD: ${DB_PASSWORD:-alumno_password}
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - backend_net

networks:
  proxy_net:
    external: true
  backend_net:
    driver: bridge

volumes:
  db_data:
  backend_storage: