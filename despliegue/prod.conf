server {
    listen 80;
    server_name proximarkt.francecentral.cloudapp.azure.com;
    index index.html index.php;

    # --- FRONTEND (VUE) ---
    # Cambiamos la ruta para que coincida con donde el Dockerfile pone los archivos
    root /var/www/html/public/build; 

    location / {
        try_files $uri $uri/ /index.html;
    }

    # --- RESTO DEL ARCHIVO (API) SE QUEDA IGUAL ---
    location /api {
        alias /var/www/html/backend/public;
        try_files $uri $uri/ @laravel;

        location ~ \.php$ {
            fastcgi_pass php:9000;
            include fastcgi_params;
            fastcgi_param SCRIPT_FILENAME /var/www/html/backend/public/index.php;
            fastcgi_param SCRIPT_NAME /index.php;
        }
    }

    location @laravel {
        rewrite ^/api/(.*)$ /api/index.php?/$1 last;
    }
}